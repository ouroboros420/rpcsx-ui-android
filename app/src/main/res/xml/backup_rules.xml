<?xml version="1.0" encoding="utf-8"?>
<!-- Used by android:fullBackupContent (API < 31). -->
<full-backup-content>
    <!-- RPCN credentials (PBKDF2-derived password + bearer token): never back up. -->
    <exclude domain="file" path="config/rpcn.yml"/>
</full-backup-content>
